apiVersion: v1
kind: Service
metadata:
  name: {{ include "daemon-cronjob.fullname" . }}-controller-manager-metrics-service
  labels:
    control-plane: controller-manager
  {{- include "daemon-cronjob.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    app.kubernetes.io/name: tmp
    control-plane: controller-manager
    {{- include "daemon-cronjob.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.metricsService.ports | toYaml | nindent 2 }}

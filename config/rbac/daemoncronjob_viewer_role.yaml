# This rule is not used by the project tmp itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to scheduling.xzzpig.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: tmp
    app.kubernetes.io/managed-by: kustomize
  name: daemoncronjob-viewer-role
rules:
- apiGroups:
  - scheduling.xzzpig.com
  resources:
  - daemoncronjobs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - scheduling.xzzpig.com
  resources:
  - daemoncronjobs/status
  verbs:
  - get
